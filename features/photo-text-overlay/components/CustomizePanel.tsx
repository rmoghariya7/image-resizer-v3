// DEAD CODE — the collapsed "Customize" advanced panel (font family, text
// color, background color/opacity, shadow, outline, corner radius, padding,
// line height, margin, letter spacing, rotation) was removed entirely per
// the simplification brief. The only remaining customization is Name/Date
// alignment + font size (NameSettingsPanel.tsx, DateSettingsPanel.tsx) and
// Footer height/background/text color (FooterSettingsPanel.tsx). The
// sandbox used to build this change could not delete tracked files
// (filesystem permission restriction) — please remove this file with
// `git rm features/photo-text-overlay/components/CustomizePanel.tsx`.
export {}
