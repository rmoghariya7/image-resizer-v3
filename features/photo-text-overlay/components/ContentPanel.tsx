// DEAD CODE — replaced by NameSettingsPanel.tsx and DateSettingsPanel.tsx,
// which each own their own enable/disable toggle, text/date input, alignment
// and font size — the old template-driven combined content panel no longer
// applies now that Name and Date are independent fields, not template-gated.
// The sandbox used to build this change could not delete tracked files
// (filesystem permission restriction) — please remove this file with
// `git rm features/photo-text-overlay/components/ContentPanel.tsx`.
export {}
