// DEAD CODE — replaced by FooterPreviewStage.tsx. This tool no longer draws
// any text on top of the uploaded photo (it is a footer generator, not a
// watermark/overlay tool) — the live preview now renders the photo untouched
// with a real DOM footer band below it. The sandbox used to build this
// change could not delete tracked files (filesystem permission restriction)
// — please remove this file with
// `git rm features/photo-text-overlay/components/OverlayPreviewStage.tsx`.
export {}
