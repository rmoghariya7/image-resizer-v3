// DEAD CODE — the Photo Footer Generator no longer uses a "Choose Template"
// step. The tool now has independent Name/Date enable-toggles instead of a
// Date/Name/Custom-Text template picker (see NameSettingsPanel.tsx and
// DateSettingsPanel.tsx). The sandbox used to build this change could not
// delete tracked files (filesystem permission restriction) — please remove
// this file with `git rm features/photo-text-overlay/components/TemplatePicker.tsx`.
export {}
