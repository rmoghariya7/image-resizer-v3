// DEAD CODE — the Photo Footer Generator removed all style presets (Minimal,
// Clean, Bold, Caption, Stamp, Transparent, Professional, Classic) and border/
// background-pill/shadow/outline options per the simplification brief: the
// tool now only exposes Footer height, Background color and Text color (see
// FooterSettingsPanel.tsx), plus per-field font size on Name/Date. The
// sandbox used to build this change could not delete tracked files
// (filesystem permission restriction) — please remove this file with
// `git rm features/photo-text-overlay/components/StylePicker.tsx`.
export {}
