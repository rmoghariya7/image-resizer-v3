// DEAD CODE — the Photo Footer Generator no longer places text ON the photo,
// so the "Where should it appear?" on-image position picker (corners/top-
// center/bottom-center) was removed. Name/Date now only ever render inside
// the footer band, positioned by their own left/center/right Alignment
// setting (see NameSettingsPanel.tsx / DateSettingsPanel.tsx). The sandbox
// used to build this change could not delete tracked files (filesystem
// permission restriction) — please remove this file with
// `git rm features/photo-text-overlay/components/LayoutPicker.tsx`.
export {}
