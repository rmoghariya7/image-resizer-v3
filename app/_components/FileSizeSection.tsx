// DEPRECATED — was never imported by the homepage (dead code found during
// the homepage information-architecture audit). Its content overlaps
// entirely with PopularGoalsSection.tsx. The sandbox used to build this
// change could not delete tracked files (filesystem permission restriction)
// — please remove this file with `git rm app/_components/FileSizeSection.tsx`.
export {}
