// DEAD CODE — this tool definition was renamed to `add-name-and-date-on-photo`
// (see ./add-name-and-date-on-photo.ts) so its registry key and route match
// the exact-match keyword/URL pattern used by the top-ranking competitors for
// "add name and date to photo" (Pi7, WebUtility.io, Pixellize.io, Pokecut).
// The old `/photo-date-stamp` URL now 301-redirects to the new route via
// next.config.ts's redirects(). The sandbox used to build this change could
// not delete tracked files (filesystem permission restriction) — please
// remove this file with `git rm registry/tools/photo-date-stamp.ts`.
export {}
